# Environment configuration for local model weights
LOCAL_MODEL_PATH=./weights
DEVICE=auto
USE_QUANTIZATION=false
LLM_SERVICE_HOST=0.0.0.0
LLM_SERVICE_PORT=8001

# Optional: HuggingFace token (only needed if fallback to HF is required)
# HUGGINGFACE_TOKEN=your_token_here
